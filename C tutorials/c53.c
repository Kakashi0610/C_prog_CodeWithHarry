// NULL POINTER - it is a type of pointer which points the NULL value which cannot be proccessed

/*
#include <stdio.h>

int main(int argc, char const *argv[])
{
    int a = 5;
    int *ptr1 = &a;
    printf("the address of pointer to variable a is : %d\n", &a); // a is : 6422296

    int b = 8;
    int *ptr2 = NULL;
    printf("the address of pointer to variable b is : %d\n", ptr2); // b is : 0

    if (ptr2 == 0)
    {
        printf("null");
    }
    else
    {
        printf("not null");
    }
    return 0;
}
*/