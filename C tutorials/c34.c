//STRINGS IN C

/*
#include <stdio.h>

void printstring(char str[]) 
{
    int i;
    while (str[i]!='\0') 
    {
        printf("%c", str[i]);
        i++;
    }
}
*/

/*
#include <stdio.h>

int main(int argc, char const *argv[])
{
    //char str[6] = {'a','n','n','u','\0'}; // for printing an array of char--> OP annu
    //char str[] = "harry";   // another way for printing an array of char--> OP harry MOST RECOMNDED 
    //printf("%s", str); //to execute normally
    //printstring(str);  //calling fuction

    //char str[20]; //is an array that can store from 0 to 19 elements of 1 byte since Char.
    //printf("using printf\n"); //allows to take the input from the user  MOST RECOMMENDED
    //gets (str); //allows to print the input from the user
    //printf("wow ! ");
    //puts("using puts !what a miracle !");  //used to add 2 string characters sentences

    return 0;
}
*/

/*OUTPUT
using printf
annu is anu
wow ! using puts !what a miracle !
*/
