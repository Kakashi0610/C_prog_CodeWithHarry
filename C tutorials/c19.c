// functions

// with args with return value
/*
#include <stdio.h>

int sum(int a, int b)
{
    return a+b;     //only write what the task is to be done- no declaring or defining
}
int main(int argc, char const *argv[])
{
    int a,b,c;
    a=6;
    b=4;
    c= sum(a,b);
    printf("the sum of a and b is: %d", c);
    return 0;
}
*/

// with args without return value

/*
#include <stdio.h>

void printstar(int i, int n) 
{
    for(i=0;i<n;i++)
    printf("%c", '*');
}

int main(int argc, char const *argv[])
{
    int i,n;
    printf("enter the value of n :%d\n");
    scanf("%d", &n);
    printstar(n);
    return 0;
}
*/

//not completed
