// STRINGS.H LIB,  STRING FUNCTIONS IN C

/*
#include <stdio.h>
#include <string.h>

int main(int argc, char const *argv[])
{
    char str[] = "harry";
    char str2[] = "\tharris";
    char str3[20];
    char str4[20];
    char str5[20];
    char str6[20];

    /*
    puts(strcat(str,str2));   //to add two strings   output---> harry \nharris
    printf("the length of str1 is : %d\n", strlen(str));  //the length of str1 is : 5
    printf("the length of str2 is : %d\n", strlen(str2));  //the length of str2 is : 7
    printf("the reversed string s is : %s\n", strrev(str));  //the reversed string s is : yrrah
    strcpy(str3,str);  //to copy the value of one string to another
    printf("str3 is now : %s", str3);  //str3 is now : yrrah
    strcpy(str3, strcat(str,str2)); // can copy a funtion executint too
    printf("the value of str3 now is : %s", str3);  //the value of str3 now is : harry        harris
    printf("the diff between str and str2 is : %d", (strcmp(str, str2))); //diff between str and str2 is : 1
    

    printf("Enter a name : ");  //kakashi
    gets (str4);
    printf("Enter a sentence : "); //kills
    gets (str5);
    printf("Enter a name : ");  //obito
    gets (str6); 
    printf(strcat(str4, strcat(str5, str6))); //kakashi kills obito
    return 0;
}
*/