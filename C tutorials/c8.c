// WAP to print multiplication table of number enterend by an user in pretty form

/*
#include <stdio.h>

int main(int argc, char const *argv[])
{
    int a,i;

    printf("Enter a number:");
    scanf("%d", &a);

    printf("%d * %d = %d\n",a,1,a*1);
    printf("%d * %d = %d\n",a,2,a*2);
    printf("%d * %d = %d\n",a,3,a*3);
    printf("%d * %d = %d\n",a,4,a*4);
    printf("%d * %d = %d\n",a,5,a*5);
    printf("%d * %d = %d\n",a,6,a*6);
    printf("%d * %d = %d\n",a,7,a*7);
    printf("%d * %d = %d\n",a,8,a*8);
    printf("%d * %d = %d\n",a,9,a*9);
    printf("%d * %d = %d\n",a,10,a*10);

    return 0;
}
*/

//    using pointer, function and for loop

/*  TRASH

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int numb(int a,int b)
{    
    return a*b;
}

int main(int argc, char const *argv[])
{
    int a,b,c,d;

    int *ptr = numb(a,b);   //main value pointed by ptr to Fnctn
    
    d = numb(a,b);
    c = d;

    printf("the value of product of two numbers is = %d\n", numb(5,7));
    
    printf("the value of product of two numbers is = %d\n",c);
    return 0;
}
*/

/*  ANOTHER TRASH
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int numb(int a,int b)
{    
    return a*b;
}

int main(int argc, char const *argv[])
{
    int a,b,c,d;

    int *ptr = numb(a,b);   //main value pointed by ptr to Fnctn

    //c = numb(a,b);
    //c = int *ptr;
    
    

    printf("the value of product of two numbers is = %d\n", numb(5,7));
    
    printf("the value of product of two numbers is = %d\n",c);
    return 0;
}
*/