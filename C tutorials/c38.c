//TYPE DEF IN C  used to give alternative name to a structure 

/*
#include <stdio.h>

typedef struct Employee   //typedef before structure name
{   
    float empmarks;  
    char name[30];  
    int empid;       
    char initial;   
} emp;     // alternate name to the structure

int main(int argc, char const *argv[])
{
    emp e1;   //can call emp as alternative of struct Employee
    e1.empmarks = 5.0;   // . is used while callingout or allocating value in a int, float, char
    printf("the marks of employee 1 is %f.", e1.empmarks);  //the value of employee 1 id is 0.000000.
    return 0;
}

*/